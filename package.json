{
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/check-site.mjs",
    "test": "node --test scripts/*.test.mjs",
    "preview": "python3 -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "markdown-it": "^15.0.2"
  }
}
